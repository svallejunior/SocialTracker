# SocialTracker — Dependências Python (Ubuntu 22.04 LTS / Windows)
# Instalação: pip install -r requirements.txt

# Core & Utilitários
python-dotenv>=1.0.0
requests>=2.28.0
pillow>=10.0.0
piexif>=1.1.3

# Coleta de Dados (Apify & Meta)
# Pinado: a partir da v2, .call() retorna um objeto Run tipado (run.default_dataset_id)
# em vez de dict (run["defaultDatasetId"]) — versão aberta já causou instalação de uma
# versão incompatível com o código em uma instalação nova.
apify-client==3.2.0
supabase>=2.0.0

# Análise de Dados & Visualização (Scripts auxiliares e Streamlit legado)
pandas>=2.0.0
plotly>=5.0.0
streamlit>=1.30.0
